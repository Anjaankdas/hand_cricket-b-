const computer = document.querySelector(".computer img");
const player = document.querySelector(".player img");
const computerPoints = document.querySelector(".computerPoints");
const playerPoints = document.querySelector(".playerPoints");
const options = document.querySelectorAll(".options button");
const option1 = document.querySelectorAll(".option1 button");


options.forEach((option) => {
  option.addEventListener("click", () => {
    computer.classList.add("shakeComputer");
    player.classList.add("shakePlayer");
    setTimeout(()=> {
       computer.classList.remove("shakeComputer");

      //  player img change
       player.classList.remove("shakePlayer");
       player.src ="./"+option.innerHTML + "player.png";

       // computer img change
       const choice = ["one", "two","three", "four","six"];
       let arrayNo = Math.floor(Math.random() * 5);
       let computerChoice = choice[arrayNo];
       computer.src = "./" + computerChoice + "Computer.png";

       // points 
       let cPoints = parseInt(computerPoints.innerHTML);
       let pPoints = parseInt(playerPoints.innerHTML);

       //player points 

      if(pPoints>=50){
         return 0;
      }
      if(option.innerHTML === "ONE")
        {
         if(computerChoice ==="ONE")
          computerPoints.innerHTML = cPoints +1;
         else 
          playerPoints.innerHTML =pPoints+1 ;
        }

       if(option.innerHTML === "TWO")
       {
        if(computerChoice !=Option.innerHTML)
        playerPoints.innerHTML = pPoints +2;
        else
        playerPoints.innerHTML = 0;
       }
       if(option.innerHTML === "THREE")
       {
        if(computerChoice !=Option.innerHTML)
        playerPoints.innerHTML = pPoints +3;
        else
        playerPoints.innerHTML = 0;
       }
       if(option.innerHTML === "FOUR")
       {
        if(computerChoice !=Option.innerHTML)
        playerPoints.innerHTML = pPoints +4;
        else
        playerPoints.innerHTML = 0;
       }
       if(option.innerHTML === "SIX")
       {
        if(computerChoice !=Option.innerHTML)
        playerPoints.innerHTML = pPoints +6;
        else
        playerPoints.innerHTML = 0;
       }

    },900);
  }) ;
});

// computerPoints

// options.forEach((option) => {
//   option.addEventListener("click", () => {
//     computer.classList.add("shakeComputer");
//     player.classList.add("shakePlayer");
//     setTimeout(()=> {
//        computer.classList.remove("shakeComputer");

//       //  player img change
//        player.classList.remove("shakePlayer");
//        player.src ="./"+option.innerHTML + "player.png";

//        // computer img change
//        const choice = ["one", "two","three", "four","six"];
//        let arrayNo = Math.floor(Math.random() * 5);
//        let computerChoice = choice[arrayNo];
//        computer.src = "./" + computerChoice + "Computer.png";

//        // points 
//        let cPoints = parseInt(computerPoints.innerHTML);
//        let pPoints = parseInt(playerPoints.innerHTML);
//       if(pPoints>=50){
//          return 0;
//       }
//        if(computerChoice == "ONE")
//        {
//         if(option.innerHTML !="ONE")
//         computerPoint.innerHTML = pPoints +1;
//         else if (computerChoice== "TWO") {
//         computerPoints.innerHTML = cPoints+2;
//         }
//         else if (computerChoice== "THREE") {
//           computerPoints.innerHTML = cPoints+2;
//           }
//        }
//        if(option.innerHTML === "TWO")
//        {
//         if(computerChoice !=Option.innerHTML)
//         playerPoints.innerHTML = pPoints +2;
//         else
//         playerPoints.innerHTML = 0;
//        }
//        if(option.innerHTML === "THREE")
//        {
//         if(computerChoice !=Option.innerHTML)
//         playerPoints.innerHTML = pPoints +3;
//         else
//         playerPoints.innerHTML = 0;
//        }
//        if(option.innerHTML === "FOUR")
//        {
//         if(computerChoice !=Option.innerHTML)
//         playerPoints.innerHTML = pPoints +4;
//         else
//         playerPoints.innerHTML = 0;
//        }
//        if(option.innerHTML === "SIX")
//        {
//         if(computerChoice !=Option.innerHTML)
//         playerPoints.innerHTML = pPoints +6;
//         else
//         playerPoints.innerHTML = 0;
//        }

//     },900);
//   }) ;
// });
